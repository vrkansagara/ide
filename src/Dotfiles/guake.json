[
  {
    "description": "List all files",
    "cmd": [
      "ls",
      "-ls"
    ]
  },
  {
    "description": "Debian update and upgrade",
    "cmd": [
      "sudo",
      "apt-get install -f && sudo apt-get clean && sudo apt-get autoremove && sudo apt-get update && sudo apt-get upgrade -V"
    ]
  },
  {
    "description": "Debian dist upgrade",
    "cmd": [
      "sudo",
      "apt-get clean && sudo apt dist-upgrade"
    ]
  },
  {
    "description": "Run phpstorm",
    "cmd": [
      "sudo",
      "ps -ef | grep 'phpstorm' | grep -v grep | awk \"{print \\$2}\" | xargs -I {} sudo kill -9 {}",
      "&& nice -n 20 ~/applications/phpstorm/bin/phpstorm &"
    ]
  },
  {
    "description": "Run Google Chrome",
    "cmd": [
      "sudo",
      "ps -ef | grep 'google-chrome' | grep -v grep | awk \"{print \\$2}\" | xargs -I {} sudo kill -9 {}",
      "&& nice -n 20 google-chrome &"
    ]
  }
]