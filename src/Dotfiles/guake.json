[
  {
    "description": "List all files",
    "cmd": [
      "ls",
      "-ls"
    ]
  },
  {
    "description": "Ubuntu update and upgrade",
    "cmd": [
      "sudo",
      "apt-get install -f && sudo update && sudo apt-get upgrade -V"
    ]
  },
  {
    "description": "Ubuntu apt clean and auto remove",
    "cmd": [
      "sudo",
      "apt-get clean && sudo apt-get autoremove"
    ]
  },
  {
    "description": "Run phpstorm",
    "cmd": [
      "sudo",
      "ps -ef | grep 'phpstorm' | grep -v grep | awk \"{print \\$2}\" | xargs -I {} sudo kill -9 {}",
      "&& ~/Applications/PhpStorm-213.7172.28/bin/phpstorm.sh"
    ]
  }
]